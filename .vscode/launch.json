{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Edge with index.html",
            "file": "${workspaceFolder}/petersmark/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}